[project]
name = "backend"
version = "0.1.0"
description = "JustAPK FastAPI backend"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.128.1",
    "requests>=2.28",
    "beautifulsoup4>=4.12",
    "lxml>=5.0",
]
